# Agent Brain: Saved Instructions

- **WhatsApp Best Practices:** Never use plain text messages (`send_whatsapp`) for E-Commerce automation branches (even after condition/wait nodes). ALWAYS use Meta pre-approved templates (`send_template`) for confirm, cancel, ship, etc. This ensures compliance with Meta's strict business messaging policies.
